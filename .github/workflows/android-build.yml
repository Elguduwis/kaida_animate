name: KAIDA Animate Android CI

on:
  push:
    branches: [ "main", "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    # STRICT ENVIRONMENT LOCK:
    # Flutter 3.13 natively aligns with our package matrix without errors.
    - uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.13.9'
        channel: 'stable'

    - run: flutter create --platforms=android .

    # The official, hack-free way to set minimum SDK for FFmpeg
    - run: echo "flutter.minSdkVersion=24" >> android/local.properties

    - run: flutter pub get

    - run: flutter build apk --release

    - uses: actions/upload-artifact@v4
      with:
        name: KAIDA-Animate-APK
        path: build/app/outputs/flutter-apk/app-release.apk
