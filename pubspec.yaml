name: kaida_animate
description: KAIDA Animate MVP - Educational Whiteboard Videos
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: ^2.0.7
  provider: ^6.0.5
  uuid: ^4.0.0
  # Pinned to avoid SDK 34 requirement crash
  path_provider: 2.0.15
  # Pinned to the last stable version that actually exists on Maven Central
  ffmpeg_kit_flutter_min_gpl: 5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
