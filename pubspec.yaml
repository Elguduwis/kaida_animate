name: kaida_animate
description: KAIDA Animate MVP
publish_to: "none"

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: ^2.0.10
  provider: ^6.1.2
  uuid: ^4.3.3
  path_provider: ^2.1.2
  # The exact, guaranteed version that exists on Maven Central
  ffmpeg_kit_flutter_min_gpl: 5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
